import 'package:flutter/material.dart';

class recentChat extends StatelessWidget {
  const recentChat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:
        Text("Here is your latest chats "),
      ),
    );
  }
}
