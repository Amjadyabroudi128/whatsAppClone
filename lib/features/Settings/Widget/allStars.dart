import 'package:flutter/material.dart';

class allStarred extends StatelessWidget {
  const allStarred({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
